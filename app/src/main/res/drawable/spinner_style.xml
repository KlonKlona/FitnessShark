<?xml version="1.0" encoding="utf-8"?>

<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <style android:name="spinner_style">
        <item android:name="android:paddingLeft">0dp</item>
        <item android:name="android:dropDownWidth">533dp</item>
        <item android:name="android:showDividers">none</item>
        <item android:name="android:dividerHeight">0dp</item>
        <item android:name="android:popupBackground">@drawable/new_bg</item>
        <item android:name="android:dropDownHeight">70dp</item>
        <item android:name="android:scrollbarAlwaysDrawVerticalTrack">true</item>
        <item android:name="android:dropDownSelector">@android:color/white</item>
    </style>
</selector>